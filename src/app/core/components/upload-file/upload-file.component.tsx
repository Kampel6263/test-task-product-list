import * as React from 'react';
import { UploadFileProps } from './upload-file.props';
import * as styles from './upload-file.scss';

/**
 * Renders UploadFile
 */
const UploadFile: React.FC<UploadFileProps> = () => {
  return (
    <div className={styles.uploadFile}>
      {/* content */}
      {/* content */}
      {/* content */}
    </div>
  );
};

export { UploadFile };
