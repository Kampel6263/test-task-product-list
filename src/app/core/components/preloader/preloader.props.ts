import { Preloaders } from '../../typescript';

/**
 * Props
 */
type PreloaderProps = {
  /**
   * Variant
   */
  variant?: string;
  /**
   * Is preloader active
   */
  isActive?: boolean;
  /**
   * Preloader id
   */
  preloaderId?: Preloaders;
};

export { PreloaderProps };
