import { HTMLAttributes } from 'react';

/**
 * Home Props
 *
 * @author Mykola Rositskyi
 * @interface
 */
type HomeProps = HTMLAttributes<HTMLDivElement>;

type InputProps = {
  name: string;
};

export type { HomeProps, InputProps };
